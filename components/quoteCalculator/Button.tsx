import React from "react";

const Button = () => {
  return (
    <button
      type="button"
      className={` px-4 border-white border-2 font w-32 h-16 rounded-full "text-slate-900

    `}
    >
      <p className={""}>Start</p>
    </button>
  );
};

export default Button;
